import json #json module for reading json files
import numpy as np #tool for creating data arrays 
import nltk #Natural languange Processing tool
from nltk.stem.lancaster import LancasterStemmer #model for stemming words / cleaning words
stemmer = LancasterStemmer()

#Loading the json data and storing it in a variable named data
with open("job_intents.json") as file:
    data = json.load(file)

#Cleaning and extracting features from the json file

#Build the models vocabulary

#Encoding the dataset
